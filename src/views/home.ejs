<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Task Manager App</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        body {
            background: url('URL_TO_YOUR_BACKGROUND_IMAGE') no-repeat center center fixed;
            background-size: cover;
            margin: 0;
            padding: 0;
            height: 100vh;
            font-family: 'Arial', sans-serif;
        }
        .navbar {
            background-color: #2c3e50;
            border-bottom: 5px solid #3498db;
            border-radius: 0;
        }
        .navbar-brand {
            color: #ecf0f1;
            font-size: 28px;
            font-weight: bold;
        }
        .content {
            background-color: rgba(255, 255, 255, 0.95);
            padding: 40px;
            border-radius: 10px;
            margin: 50px auto;
            max-width: 600px;
            text-align: center;
            box-shadow: 0px 0px 20px rgba(0, 0, 0, 0.4);
        }
        .btn-primary {
            background-color: #3498db;
            border-color: #3498db;
        }
        .btn-primary:hover {
            background-color: #2980b9;
            border-color: #2980b9;
        }
        .btn-secondary {
            background-color: #e74c3c;
            border-color: #e74c3c;
        }
        .btn-secondary:hover {
            background-color: #c0392b;
            border-color: #c0392b;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg">
        <div class="container">
            <a class="navbar-brand" href="#">Task Manager</a>
        </div>
    </nav>
    <div class="container content">
        <h1>Welcome to Task Manager</h1>
        <p>Your personalized task management platform.</p>
        <div class="mt-4">
            <a href="/login" class="btn btn-primary btn-lg mr-2">Login</a>
            <a href="/signup" class="btn btn-secondary btn-lg">Register</a>
        </div>
    </div>
</body>
</html>
